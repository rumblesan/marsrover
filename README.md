# Mars Rover

```
docker build -t marsrover .
docker run -it --name exploremars marsrover stack exec marsrover
```

```
docker run --name exploremars marsrover stack exec marsrover
```

```
docker rm exploremars
docker rm testmars
```

